name: Test Action

on:
  push:
    branches:
      - main  # Change this if you want to trigger the action on another branch

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Print message
        run: echo "Hello, world!"
